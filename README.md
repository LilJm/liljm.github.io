## Rodar localmente

**Pre-requisitos:**  Node.js


1. Instale as dependências:
   `npm install`
2. Defina a `GEMINI_API_KEY` em [.env.local](.env.local) com a sua Gemini API key
3. Rode o App:
   `npm run dev`
